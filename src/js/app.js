$('.slider-feedback').slick();

lightGallery(document.getElementById('lightgallery'));

$(function () {
	$(".team-img").jqZoom({
		selectorWidth: 30,
		selectorHeight: 30,
		viewerWidth: 400,
		viewerHeight: 400
	});
})